(jbuild_version 1)


(executables
 ((names        (plotgitsch plotkicadsch))
  (public_names (plotgitsch plotkicadsch))
  (package plotkicadsch)
  (preprocess (pps (lwt.ppx ppx_hash ppx_compare ppx_sexp_conv)))
 (libraries
  (
   kicadsch
   tyxml
   git-unix
   lwt
   lwt.unix
   lwt.ppx
   sha
   str
   base64
   patience_diff
   ppx_hash
   ppx_compare
   ppx_sexp_conv
   )
  )
  (flags (:standard -w -3 -safe-string))
))

(install
 ((package plotkicadsch)
  (section bin)
   (files   (git-imgdiff))
  ))
